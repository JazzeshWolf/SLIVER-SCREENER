{
  "name": "sliver-screener",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCX Silver options-seller dashboard — multi-horizon sentiment, IV/premium-sell signals, basis & expiry tracking.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:data": "node scripts/build-data.mjs"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^26.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
